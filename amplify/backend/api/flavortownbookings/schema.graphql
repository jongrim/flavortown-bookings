type Booking
  @model
  @key(
    name: "bookingsByDate"
    fields: ["date", "time"]
    queryField: "bookingsByDate"
  ) {
  id: ID!
  name: String
  email: AWSEmail
  partySize: Int
  date: AWSDate!
  time: AWSTime!
}
